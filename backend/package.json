{
  "name": "libreria-backend",
  "version": "1.0.0",
  "description": "Backend del sistema de gestión de librería (Express + Prisma + SQLite)",
  "main": "server.js",
  "scripts": {
    "start": "node server.js",
    "dev": "nodemon server.js",
    "test": "node --test",
    "prisma:generate": "prisma generate",
    "prisma:push": "prisma db push",
    "prisma:studio": "prisma studio",
    "seed:admin": "node prisma/seed-admin.js",
    "migrar:exportar-disponibilidad": "node scripts/exportar-disponibilidad.js",
    "migrar:cantidad-disponible": "node scripts/migrar-cantidad-disponible.js"
  },
  "prisma": {
    "seed": "node prisma/seed.js"
  },
  "dependencies": {
    "@prisma/client": "^6.5.0",
    "bcrypt": "^5.1.1",
    "cors": "^2.8.5",
    "dotenv": "^16.4.5",
    "express": "^4.19.2",
    "jsonwebtoken": "^9.0.2"
  },
  "devDependencies": {
    "nodemon": "^3.1.4",
    "prisma": "^6.5.0"
  }
}
